document.querySelector("#loginForm").addEventListener("submit",e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));const arr=JSON.parse(localStorage.getItem("bytefest_registrations")||"[]");const found=arr.find(x=>x.email.toLowerCase()===d.email.toLowerCase()&&x.registrationId.toLowerCase()===d.registrationId.toLowerCase());if(!found){document.querySelector("#loginError").textContent="Registration not found. Check your email and ID.";return}localStorage.setItem("bf_current",JSON.stringify(found));location.href="dashboard.html";});